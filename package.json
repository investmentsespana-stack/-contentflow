{
  "name": "contentflow-master",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test:e2e": "playwright test",
    "typecheck:approval-crypto": "tsc --noEmit --strict --target ES2022 --module Node16 --moduleResolution Node16 src/platform/approval-crypto-validator.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "6.0.3"
  }
}
