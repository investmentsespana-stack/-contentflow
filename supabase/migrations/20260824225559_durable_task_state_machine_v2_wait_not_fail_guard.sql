-- Historical lineage reconstruction marker.
-- Wait-not-fail semantics are represented by the current typed finalization,
-- retry classification, durable wait registry and causal wake reconciliation.
select 'durable_task_state_machine_v2_wait_not_fail_guard lineage restored'::text;
