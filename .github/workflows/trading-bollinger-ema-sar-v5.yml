name: Trading Bollinger EMA SAR V5

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'trading_super_strategy/bollinger_ema_sar_v5_spec.json'
      - 'trading_super_strategy/bollinger_ema_sar_v5_backtest.py'
      - 'trading_super_strategy/bollinger_ema_sar_v5_consolidate.py'
      - '.github/workflows/trading-bollinger-ema-sar-v5.yml'

permissions:
  contents: read
  actions: read

jobs:
  backtest:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        symbol: ['ES.v.0','NQ.v.0','YM.v.0','RTY.v.0']
    env:
      DATABENTO_API_KEY: ${{ secrets.DATABENTO_API_KEY }}
      BES_SYMBOL: ${{ matrix.symbol }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install research dependencies
        run: python -m pip install --upgrade databento pandas numpy
      - name: Compile V1 base and V5 strategy
        run: python -m py_compile trading_super_strategy/bollinger_ema_sar_v1_backtest.py trading_super_strategy/bollinger_ema_sar_v5_backtest.py
      - name: Run V5 discovery-only 1m backtest
        working-directory: trading_super_strategy
        run: python bollinger_ema_sar_v5_backtest.py
      - name: Upload V5 market evidence
        uses: actions/upload-artifact@v4
        with:
          name: bes-v5-${{ matrix.symbol }}
          path: trading_super_strategy/bollinger_ema_sar_v5_output/
          if-no-files-found: error
          retention-days: 30

  consolidate:
    needs: backtest
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install consolidation dependencies
        run: python -m pip install pandas numpy
      - name: Download all V5 market artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: bes-v5-*
          path: trading_super_strategy/bollinger_ema_sar_v5_downloads
      - name: Consolidate V5 cross-market results
        working-directory: trading_super_strategy
        run: python bollinger_ema_sar_v5_consolidate.py
      - name: Upload V5 cross-market evidence
        uses: actions/upload-artifact@v4
        with:
          name: bes-v5-crossmarket
          path: trading_super_strategy/bollinger_ema_sar_v5_crossmarket/
          if-no-files-found: error
          retention-days: 30
